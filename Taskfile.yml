version: '3'

tasks:
  css:
    desc: Build css
    cmds:
      - tailwindcss -i ./assets/tw.css -o ./assets/styles.css

  build:
    desc: Generate templates and index.html
    cmds:
      - GOARCH=wasm GOOS=js go build -o web/app.wasm src/*.go
      - go build -o run src/*.go

  run:
    desc: Run application
    deps:
      - build
    cmds:
      - ./run
